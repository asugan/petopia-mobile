import { StyleSheet } from 'react-native';

export const subscriptionStyles = StyleSheet.create({
  note: {
    marginTop: 4,
    marginBottom: 16,
    fontSize: 12,
    lineHeight: 16,
    textAlign: 'center',
    opacity: 0.8,
  },
});
